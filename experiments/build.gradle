plugins {
    id "cpp-application"
}

application {
    baseName = "experiments"

    dependencies {
        implementation project(':slicing')
        implementation project(':baseline')
    }

    source.from file("src")
}